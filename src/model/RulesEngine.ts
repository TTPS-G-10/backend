import { Alert } from "./Alert";
import { Rule } from "./Rule";

export class RulesEngine {
  rules: Rule[];
  constructor() {
    this.rules = [];
  }

  constructor{
  
  }
  //me quedo con todas las reglas que esten prendidas-->filter

  //evaluo una por una las condiciones de las reglas a ver cual cumple
  // si se cumple la regla creo una alerta
alert = new Alert();//

}
